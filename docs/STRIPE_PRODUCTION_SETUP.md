# ChinaBound Travel ? Stripe Production Setup & Secret Rotation (P0.5)

> ?????2026-08-12
> ?????????? secret????? `<your_value>` ???

## 1. ?? Secret ?????????

| Secret | ???? | ???? | ???? |
|---|---|---|---|
| `STRIPE_SECRET_KEY` | `functions/api/checkout.js:36` (`env.STRIPE_SECRET_KEY`) | Cloudflare Pages env | Cloudflare Pages ??? Secret?? `wrangler.toml` vars? |
| `STRIPE_WEBHOOK_SECRET` | `functions/api/stripe-webhook.js:29` (`env.STRIPE_WEBHOOK_SECRET`) | Cloudflare Pages env | ?? |
| `RESEND_API_KEY` | `functions/api/stripe-webhook.js:30` (`env.RESEND_API_KEY`) | Cloudflare Pages env | ?? |
| `EBOOK_URL`???? | `functions/api/stripe-webhook.js:31` (`env.EBOOK_URL`) | Cloudflare Pages env | ?? |
| `PROCESSED_EVENTS`?KV? | `functions/api/stripe-webhook.js:51` (`env.PROCESSED_EVENTS`) | Cloudflare KV binding | Cloudflare ????? KV namespace ???? Pages ?? |

???????????????? secret????? `env.*` ????????git ?? `.env`?`stripe-webhook.js` ???????????????????? ?4 ??????

## 2. ?????????

? **Cloudflare Pages ???chinaboundtravel?? Settings ? Environment variables** ????

| ?? | ?? | ?? |
|---|---|---|
| `STRIPE_WEBHOOK_SECRET` | ? | Stripe Webhook ?????`whsec_...`???? webhook ??? |
| `STRIPE_SECRET_KEY` | ? | Stripe ??????`sk_live_...`??`checkout.js` ?? Checkout Session ? |
| `RESEND_API_KEY` | ? | Resend ?? API ???`re_...`????????????? |
| `EBOOK_URL` | ? | ??? PDF ???????????? |
| `PROCESSED_EVENTS` | ????? | KV namespace ?????????? Resend `Idempotency-Key` ?? |

> ???`wrangler.toml` ?? `[vars]` ???????????????????????? Cloudflare Pages ? **Secret**??????????? `wrangler.toml`?

## 3. ????? KV???????

1. Cloudflare ??? ? Workers & Pages ? KV ? ?? namespace??? `chinaboundtravel-processed-events`?
2. Pages ?? ? Settings ? Bindings ? Add KV namespace binding?
   - ????`PROCESSED_EVENTS`
   - ???`chinaboundtravel-processed-events`
3. ?????`stripe-webhook.js`??
   - ?????? `evt:<event.id>`??? ? ?? `{received: true, duplicate: true}`?**?????????**?
   - ??????????? `evt:<event.id>`?TTL 7 ???
   - ??? KV ? KV ??? ? ?????Resend ??? `Idempotency-Key: stripe-<event.id>`?? Resend ????????????????
   - ???????????????? `checkout.session.completed` ?????????????

## 4. Secret Rotation Checklist????????

**???git ???commit `0632b32`?`a4e696a`???? `.env`???? `.env` ?? `STRIPE_SECRET_KEY` / `STRIPE_WEBHOOK_SECRET` / `RESEND_API_KEY` ???????? ? ??? key ???????????**

### 4.1 ?????Stripe ???

1. **Stripe Dashboard ? Developers ? Webhooks**????? webhook ?? ? *Reveal* ???????? ? ?? **Rotate**????? `whsec_...`?????????
2. **Stripe Dashboard ? Developers ? API keys**?Rotate ?? `sk_live_...` ??????? key ????????? webhook secret??
3. **Cloudflare Pages ? chinaboundtravel ? Settings ? Environment variables**??? `STRIPE_WEBHOOK_SECRET`?????`STRIPE_SECRET_KEY`??????`RESEND_API_KEY`?Resend ????????????
4. **????**???????? `deploy-cloudflare-pages.yml`?workflow_dispatch??? secret ???
5. **?? `.env`**?????????????????/`wrangler dev` ???
6. **??**?? ?5?

### 4.2 ???????

- Resend API key?`re_...`??Resend ??? ? API Keys ? ??? key ? ?? Cloudflare + ?? `.env`?
- Google API key?`AIza...`??? `joran_blog_generator.py` ????Google Cloud Console ? ??/??? key?
- ??? OAuth ????? `video-pipeline`????????????????
- Feishu webhook URL?????/???????????????????? webhook?? Feishu ??????? URL ???

### 4.3 ?????

- ?? `.env` ??????**??**??? sha256 ??????????
- `git log --all` ??????? `whsec_`/`sk_live_`/`re_`/`AIza` ??filter-repo ?????
- GitHub ? `origin/trae/*` ??????? commit ??????????????

## 5. ????

### 5.1 ???????????

```bash
node --test tests/stripe_webhook_idempotency.test.mjs
# ???8 passed?????? 400???? 3 ???? 1 ????
```

### 5.2 ?? Webhook?????????

1. ?? Stripe CLI?`stripe login` ?
   ```bash
   stripe listen --forward-to localhost:8788/api/stripe-webhook
   ```
2. ???? Functions?`wrangler pages dev` ? `npx wrangler pages dev public`???? `.env` ???? secret?
3. ???????**??**?????????
   ```bash
   stripe trigger checkout.session.completed
   ```
4. ???`200 {received: true}`?????? 1 ????????????????Stripe CLI ???????????? `200 {received: true, duplicate: true}`?**????**?

### 5.3 ?????????

- Stripe Dashboard ? Webhooks ? ???????? `200`???????`400 invalid signature` ?? secret ?????
- ?? KV namespace ? `evt_*` ?????????? `evt_*` ?????
- ?? Resend ?????????? 1 ????

### 5.4 ??????

- KV?`wrangler kv key list --binding=PROCESSED_EVENTS`???????? `evt:` ????
- Resend?? `Idempotency-Key: stripe-<eventId>` ???????
- Webhook ???/???`duplicate: true` ?? = ????????

## 6. ?? webhook ????

- ?? URL?`https://www.chinaboundtravel.com/api/stripe-webhook`
- ??????? `checkout.session.completed`???????????????
- ????????????????????

## 7. ????

- ?? secret ???? `wrangler.toml`?`hugo.toml`??? HTML???? git?
- ??????? secret??? `.env` ??? `.gitignore`?
