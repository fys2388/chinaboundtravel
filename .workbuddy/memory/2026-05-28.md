# 2026-05-28 工作日志

## Stripe Checkout 支付链路打通

### 完成事项
1. **Cloudflare Pages Secrets 自动注入**（REST API 方案）
   - 脚本：`cf-pages-env-via-api.ps1`
   - 注入变量：STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, RESEND_API_KEY, SUCCESS_URL, CANCEL_URL
   - Token：`WorkBuddy-CI`（无 IP 白名单，Pages Edit 权限）

2. **Functions 路由踩坑与修复**
   - **失败方案**：`functions/api/checkout.js` — wrangler 4.94.0 下路由注册失败，多次部署均 404
   - **成功方案**：改用 `static/_worker.js`（单 Worker 方案），wrangler 正确识别 `Uploading Worker bundle`
   - `env.ASSETS.fetch(request)` 回退机制正常服务静态文件

3. **支付验证**
   - 预览地址 `https://ad9c9826.chinaboundtravel.pages.dev/api/checkout` 返回 `{"error":"Missing plan parameter"}`
   - 主页正常加载，站点无破坏

### 技术细节
- `_worker.js` 导出格式：`export default { async fetch(request, env, executionCtx) {...} }`
- wrangler.toml 干扰：上层目录 `E:\AI\dulizhan\wrangler.toml` 有 `name` 字段导致 wrangler 混淆，已清理为仅保留 `compatibility_date`
- 部署命令：`hugo --gc --minify && npx wrangler pages deploy public --project-name chinaboundtravel --branch main`

### 2026-05-28 晚更新
4. **Pricing 页购买按钮不稳定问题**
   - 现象：偶尔成功跳转 Stripe Checkout，大部分时候 "Checkout unavailable"
   - 根因：Cloudflare 边缘节点 Secrets 最终一致性——某些节点已同步 STRIPE_SECRET_KEY，某些节点还没同步
   - 修复：`_worker.js` 改用 Stripe Price ID（`price: planConfig.priceId`）代替内联 `price_data` 创建
   - 新预览地址：`https://b951aab6.chinaboundtravel.pages.dev`
   - 建议：预览地址测试（无 CDN 缓存干扰），等 2-3 分钟边缘节点同步后主域名也会稳定

### 待完成
- Stripe Dashboard 配置 Webhook URL：`https://chinaboundtravel.com/api/stripe-webhook`
- 测试 Pricing 页购买按钮 → Stripe Checkout 完整流程（预览地址）
- Resend 邮件发送逻辑完善（目前为 501 placeholder）
